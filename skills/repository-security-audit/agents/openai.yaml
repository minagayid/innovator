interface:
  display_name: "Repository Security Audit"
  short_description: "Audit repository security and release readiness"
  default_prompt: "Use $repository-security-audit to review these repositories for security and release readiness."
policy:
  allow_implicit_invocation: true