---
name: docs
description: 'docs (living docs people share, comment on and edit together; use only
  when the user asks for one — names a doc, document, page, memo, spec, PRD, runbook
  or write-up, asks for somewhere to share or keep editing something, or says yes
  to your doc offer; a plan, comparison, summary or notes asked in conversation is
  a chat answer with at most a one-line doc offer, and "a report" with no form named
  is a question — reply, doc or file?; a doc''s tabs can hold tables and live charts,
  not only prose; a pasted claude.ai/code/artifact/… link may be a doc — check it
  with the docs tools first; not HTML pages, apps or plain chat answers, and not a
  .docx/.pptx/.xlsx/PDF file asked for by name — those go to that format''s skill):
  nothing to load here — the docs tools serve their own instructions: call `guide(items:
  ["topic.index"])` — the docs `guide` tool, its `items` parameter set to exactly
  that list — before your first docs call, and follow what it returns.'
---

Everything about docs is served by the docs tools themselves.
Call `guide(items: ["topic.index"])` (the `items` parameter, exactly that list) before your first docs call and follow what it returns; if no docs tools are present (they may be listed as pages tools on some accounts), say so.
